feat(student-ui): stage 0+1 polish — design tokens, shared bars, chat/history/login rewrite

Stage 0 — foundation:
- Add apps/student-app/src/styles/global.scss with shared utilities
  (animate-fade-up, delay-1..10, line-clamp, typing-dots, blink-cursor,
  press-shrink, Material Symbols base) mirrored from teacher-app.
- Add apps/student-app/src/components/TopAppBar.vue: 56px fixed top
  bar with blur backdrop, slot-driven right area, optional badge,
  back/action emits. Replaces 4 different bespoke top-nav patterns.
- Upgrade apps/student-app/src/components/CustomTabBar.vue: 64px
  height, blur(20px) saturate(180%), active dot indicator, Material
  Symbols FILL/wght variation on active, :active scale feedback,
  per-tab badge slot.
- App.vue: import global.scss alongside tokens.scss + theme.scss.
- styles/theme.scss: drop dead variables, keep $transition-base /
  $transition-spring + $primary-10..95 legacy palette aliases that
  point back into the canonical token system.
- uni.scss: realign uni-app builtin variables to v2 violet-800
  (#5B21B6) and gray-900 text.
- pages.json: globalStyle background and tabBar selectedColor moved
  off the legacy #7C3AED to canonical #5B21B6.
- index.html: add viewport-fit=cover bootstrap so safe-area-inset
  resolves on H5, plus preconnect for fonts.gstatic.com.

Stage 1 — core interactive pages:
- pages/chat/index.vue: full SCSS rewrite under design tokens.
  - Top nav replaced with TopAppBar (history action button).
  - Welcome empty state: 64x64 gradient logo with rotation, dual
    primary-tinted shadow, radial glow, fade-up entrance.
  - AI bubble: surface with elevation shadow + soft primary border
    instead of left-edge accent; teacher bubble mirrors with success
    palette.
  - Markdown styling split into .markdown-body (base, used inline)
    and .markdown-body--rich (code/blockquote/table, used in source
    sheet) so both reuse the same color tokens.
  - Citations bg switched to $primary-soft; cit-item gains :active
    scale + shadow.
  - Suggestion chips fully tokenised, with primary-soft :active.
  - Bottom input bar repositioned above CustomTabBar; input gains
    focus-within ring; send button is gradient + scale feedback.
  - Inline call-teacher button is pill + primary shadow; done state
    uses success alpha.
  - Source sheet overlay/header/icons all token-driven.
  - Removed unused goBack handler now that TopAppBar handles back.
- pages/chat/history.vue: full rewrite.
  - Empty state: 88x88 primary-soft circle with filled forum icon
    and guidance copy.
  - Conversation card refactored to icon-wrap + body two-column
    layout; status icon-wrap has 5 colour variants (primary, warning,
    success, neutral, muted) selected by status.
  - status-badge gets 5 BEM modifiers + leading status-dot.
  - Card :active shrinks + deepens shadow.
  - Stepped fade-up entrance via delay-1..6.
  - Loader uses .typing-dots; no-more uses centered em-dash copy.
- pages/login/index.vue: glassmorphism upgrade.
  - Three-layer orb decor (white / violet-400 / violet-300) over a
    primary -> primary-hover -> violet-950 gradient page bg.
  - Card uses rgba($bg-card, 0.96) + backdrop-filter blur(40px)
    saturate(180%) + inset highlight.
  - Logo 88x88 gradient + 3deg rotation + dual primary shadow with
    -3deg counter-rotated icon inside.
  - Title 30px / 800 / -0.02em letter-spacing; bilingual labels
    (e.g. "学号 / STUDENT ID") in 11px uppercase 0.10em.
  - Inputs gain focus-within primary border + 4px primary glow ring.
  - Submit button is pill gradient with dual primary shadow + scale
    feedback.
  - Card and footer animate-fade-up with delay-2 stagger.

Spec compliance verified:
- Zero hard-coded violet hex (#630ed4 / #7c3aed / #702ae1) in chat,
  history, login. The single intentional exception is #047857
  (emerald-700, canonical darker shade of $success), called out
  inline because tokens.scss has no $success-hover yet.
- All spacing / radius / font-size / shadow values are token refs.
- safe-area-inset-* respected on every fixed bar (top + bottom).
- :active feedback present on every clickable card / button.

Plan + spec persisted at .tasks/student-ui-polish/PLAN.md so the
remaining stage 2 (home/services/profile) and stage 3 (teacher-app
T1-T4 polish) can be picked up in a follow-up conversation without
context loss.

Out of scope (left for stage 2 in next session):
- pages/home/index.vue (largely modern but needs token convergence
  + entrance animation)
- pages/services/index.vue (heavy reliance on hard-coded hex and a
  parallel utility-class system that conflicts with tokens)
- pages/profile/index.vue (hero gradient + settings icon colours
  still hard-coded; also needs TopAppBar adoption)
