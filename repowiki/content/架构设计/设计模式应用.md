# 设计模式应用

<cite>
**本文档引用的文件**
- [state_machine.py](file://services/gateway/app/services/state_machine.py)
- [ws_manager.py](file://services/gateway/app/services/ws_manager.py)
- [deps.py](file://services/gateway/app/utils/deps.py)
- [conversation.py](file://services/gateway/app/models/conversation.py)
- [conversation_service.py](file://services/gateway/app/services/conversation_service.py)
- [chat.py](file://services/gateway/app/routers/chat.py)
- [ws.py](file://services/gateway/app/routers/ws.py)
- [main.py](file://services/gateway/app/main.py)
- [websocket.ts](file://apps/student-app/src/utils/websocket.ts)
- [websocket.ts](file://apps/teacher-app/src/utils/websocket.ts)
- [user.ts](file://apps/student-app/src/stores/user.ts)
- [index.vue](file://apps/student-app/src/pages/chat/index.vue)
- [index.vue](file://apps/teacher-app/src/pages/dashboard/index.vue)
</cite>

## 目录
1. [引言](#引言)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖分析](#依赖分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)

## 引言

医小管 v2 项目是一个基于 FastAPI 的智能校园助手系统，采用了多种设计模式来实现复杂的功能需求。本文档深入分析项目中应用的设计模式，包括状态机模式、依赖注入模式、观察者模式和工厂模式，并提供具体的应用场景和最佳实践指导。

## 项目结构

医小管 v2 项目采用前后端分离架构，主要分为以下层次：

```mermaid
graph TB
subgraph "前端应用"
StudentApp[学生端应用]
TeacherApp[教师端应用]
end
subgraph "网关服务"
FastAPI[FastAPI 应用]
Routers[路由层]
Services[业务服务层]
Utils[工具层]
Models[数据模型层]
end
subgraph "基础设施"
Database[(PostgreSQL)]
Redis[(Redis)]
Dify[Dify AI 服务]
end
StudentApp --> FastAPI
TeacherApp --> FastAPI
FastAPI --> Routers
Routers --> Services
Services --> Database
Services --> Redis
Services --> Dify
```

**图表来源**
- [main.py:1-78](file://services/gateway/app/main.py#L1-L78)
- [chat.py:1-191](file://services/gateway/app/routers/chat.py#L1-L191)

**章节来源**
- [main.py:1-78](file://services/gateway/app/main.py#L1-L78)
- [chat.py:1-191](file://services/gateway/app/routers/chat.py#L1-L191)

## 核心组件

### 状态机服务
状态机模式在会话管理系统中发挥着关键作用，实现了复杂的业务状态流转控制。

### WebSocket 管理器
ConnectionManager 类实现了观察者模式，负责管理 WebSocket 连接和消息广播。

### 依赖注入系统
FastAPI 的依赖注入机制提供了灵活的服务解析和生命周期管理。

### 实时通信框架
前端和后端都实现了观察者模式，支持实时消息传递和状态同步。

**章节来源**
- [state_machine.py:1-96](file://services/gateway/app/services/state_machine.py#L1-L96)
- [ws_manager.py:1-100](file://services/gateway/app/services/ws_manager.py#L1-L100)
- [deps.py:1-40](file://services/gateway/app/utils/deps.py#L1-L40)

## 架构概览

系统采用分层架构，每层都有明确的职责分工：

```mermaid
graph TB
subgraph "表现层"
Frontend[Vue.js 前端]
WebSocket[WebSocket 客户端]
end
subgraph "应用层"
API[FastAPI API]
DI[依赖注入]
Auth[认证中间件]
end
subgraph "业务层"
StateMachine[状态机]
ConversationService[会话服务]
WSManager[WebSocket 管理]
end
subgraph "数据层"
PostgreSQL[(PostgreSQL)]
Redis[(Redis 缓存)]
end
Frontend --> API
WebSocket --> API
API --> DI
DI --> Auth
Auth --> StateMachine
StateMachine --> ConversationService
ConversationService --> WSManager
WSManager --> WebSocket
ConversationService --> PostgreSQL
StateMachine --> Redis
```

**图表来源**
- [main.py:1-78](file://services/gateway/app/main.py#L1-L78)
- [ws_manager.py:1-100](file://services/gateway/app/services/ws_manager.py#L1-L100)
- [state_machine.py:1-96](file://services/gateway/app/services/state_machine.py#L1-L96)

## 详细组件分析

### 状态机模式应用

#### 状态定义
会话状态机定义了完整的状态流转体系：

```mermaid
stateDiagram-v2
[*] --> AI_Serving : 初始状态
AI_Serving --> Pending_Teacher : escalate(学生请求转人工)
AI_Serving --> Closed : close(直接关闭)
Pending_Teacher --> Teacher_Serving : accept(教师接入)
Pending_Teacher --> AI_Serving : timeout(超时回退)
Pending_Teacher --> Closed : close(关闭)
Teacher_Serving --> Resolved : resolve(问题解决)
Teacher_Serving --> Closed : close(关闭)
Resolved --> AI_Serving : reactivate(重新激活)
Resolved --> Closed : close(最终关闭)
note right of AI_Serving
AI 自动服务状态
可直接关闭
end note
note right of Pending_Teacher
等待教师接入
可超时回退
end note
note right of Teacher_Serving
教师人工服务
可标记解决
end note
note right of Resolved
已解决状态
可重新激活
end note
```

**图表来源**
- [state_machine.py:19-31](file://services/gateway/app/services/state_machine.py#L19-L31)
- [conversation.py:11-16](file://services/gateway/app/models/conversation.py#L11-L16)

#### 状态转换规则
状态转换通过严格的规则表控制：

| 当前状态 | 动作 | 目标状态 | 条件 |
|---------|------|----------|------|
| ai_serving | escalate | pending_teacher | 学生请求转人工 |
| ai_serving | close | closed | 直接关闭 |
| pending_teacher | accept | teacher_serving | 教师接入 |
| pending_teacher | timeout | ai_serving | 超时回退 |
| pending_teacher | close | closed | 关闭 |
| teacher_serving | resolve | resolved | 问题解决 |
| teacher_serving | close | closed | 关闭 |
| resolved | reactivate | ai_serving | 学生继续提问 |
| resolved | close | closed | 最终关闭 |

#### 状态持久化机制
状态转换不仅更新数据库状态，还记录系统消息：

```mermaid
sequenceDiagram
participant Client as 客户端
participant API as API 层
participant Service as 业务服务
participant DB as 数据库
participant WS as WebSocket
Client->>API : 触发状态转换
API->>Service : transition(conv, action)
Service->>DB : 更新会话状态
Service->>DB : 插入系统消息
Service->>WS : 广播状态变更
WS->>Client : 推送状态更新
Service->>DB : 提交事务
Service-->>API : 返回新状态
API-->>Client : 确认响应
```

**图表来源**
- [state_machine.py:34-96](file://services/gateway/app/services/state_machine.py#L34-L96)
- [chat.py:43-51](file://services/gateway/app/routers/chat.py#L43-L51)

**章节来源**
- [state_machine.py:1-96](file://services/gateway/app/services/state_machine.py#L1-L96)
- [conversation.py:1-63](file://services/gateway/app/models/conversation.py#L1-L63)
- [chat.py:1-191](file://services/gateway/app/routers/chat.py#L1-L191)

### 依赖注入模式应用

#### 依赖解析机制
FastAPI 的依赖注入系统提供了强大的服务解析能力：

```mermaid
classDiagram
class DependencyResolver {
+get_current_user() User
+get_db() AsyncSession
+get_redis() Redis
}
class JWTAuth {
+decode_access_token() dict
+verify_token() bool
}
class DatabaseManager {
+get_db() AsyncSession
+execute() Result
}
class RedisManager {
+get_redis() Redis
+cache_data() void
}
DependencyResolver --> JWTAuth : 使用
DependencyResolver --> DatabaseManager : 使用
DependencyResolver --> RedisManager : 使用
```

**图表来源**
- [deps.py:14-39](file://services/gateway/app/utils/deps.py#L14-L39)
- [main.py:16-22](file://services/gateway/app/main.py#L16-L22)

#### 生命周期管理
依赖注入提供了完善的生命周期管理：

```mermaid
flowchart TD
Start([应用启动]) --> InitDeps["初始化依赖"]
InitDeps --> CreateDB["创建数据库连接池"]
CreateDB --> CreateRedis["创建 Redis 连接"]
CreateRedis --> Ready["服务就绪"]
Ready --> Request["处理请求"]
Request --> ResolveDeps["解析依赖"]
ResolveDeps --> Process["执行业务逻辑"]
Process --> Cleanup["清理资源"]
Cleanup --> Ready
Ready --> Shutdown["应用关闭"]
Shutdown --> CloseDB["关闭数据库连接"]
CloseDB --> CloseRedis["关闭 Redis 连接"]
CloseRedis --> End([结束])
```

**图表来源**
- [main.py:16-22](file://services/gateway/app/main.py#L16-L22)
- [deps.py:14-39](file://services/gateway/app/utils/deps.py#L14-L39)

**章节来源**
- [deps.py:1-40](file://services/gateway/app/utils/deps.py#L1-L40)
- [main.py:1-78](file://services/gateway/app/main.py#L1-L78)

### 观察者模式实现

#### WebSocket 连接管理
ConnectionManager 类实现了观察者模式的核心功能：

```mermaid
classDiagram
class ConnectionManager {
-user_connections : Dict[int, Set[WebSocket]]
-room_connections : Dict[str, Set[WebSocket]]
-ws_user_map : Dict[WebSocket, int]
+connect(ws, user_id) void
+disconnect(ws) void
+join_room(ws, room_id) void
+leave_room(ws, room_id) void
+send_to_user(user_id, message) void
+broadcast_to_room(room_id, message) void
+broadcast_to_college_teachers(college_id, teacher_ids, message) void
}
class WsManager {
-listeners : Map[string, Set[Callback]]
-joinedRooms : Set[number]
-sendQueue : object[]
+on(type, callback) void
+off(type, callback) void
+joinRoom(convId) void
+leaveRoom(convId) void
+send(data) void
+dispatch(type, data) void
}
ConnectionManager --> WebSocket : 管理
WsManager --> Callback : 观察
```

**图表来源**
- [ws_manager.py:8-99](file://services/gateway/app/services/ws_manager.py#L8-L99)
- [websocket.ts:3-152](file://apps/student-app/src/utils/websocket.ts#L3-L152)

#### 实时通信流程
观察者模式确保了实时通信的可靠性：

```mermaid
sequenceDiagram
participant Student as 学生客户端
participant Teacher as 教师客户端
participant WS as WebSocket 服务器
participant Manager as 连接管理器
participant DB as 数据库
Student->>WS : 发送消息
WS->>Manager : 广播到房间
Manager->>Teacher : 推送消息
Manager->>Student : 确认接收
Note over Student,Teacher : 状态变更通知
Student->>WS : 请求状态变更
WS->>Manager : 更新状态
Manager->>Teacher : 推送状态变更
Manager->>Student : 推送状态变更
WS->>DB : 持久化状态
```

**图表来源**
- [ws_manager.py:59-81](file://services/gateway/app/services/ws_manager.py#L59-L81)
- [ws.py:62-106](file://services/gateway/app/routers/ws.py#L62-L106)

**章节来源**
- [ws_manager.py:1-100](file://services/gateway/app/services/ws_manager.py#L1-L100)
- [ws.py:1-119](file://services/gateway/app/routers/ws.py#L1-L119)
- [websocket.ts:1-153](file://apps/student-app/src/utils/websocket.ts#L1-L153)

### 工厂模式应用

#### 会话服务工厂
会话服务提供了多种工厂方法来创建不同类型的会话：

```mermaid
classDiagram
class ConversationService {
<<factory>>
+create_conversation(student, title) Conversation
+get_conversation(conv_id, user) Conversation
+list_conversations(user, page, size, status) tuple
+add_message(conv_id, sender_type, content, sender_id) Message
}
class ConversationFactory {
<<factory>>
+create_ai_serving(student, title) Conversation
+create_pending_teacher(student, title) Conversation
+create_with_status(student, title, status) Conversation
}
class MessageFactory {
<<factory>>
+create_system_message(content) Message
+create_student_message(content, sender_id) Message
+create_ai_message(content) Message
+create_teacher_message(content, sender_id) Message
}
ConversationService --> ConversationFactory : 使用
ConversationService --> MessageFactory : 使用
```

**图表来源**
- [conversation_service.py:29-179](file://services/gateway/app/services/conversation_service.py#L29-L179)

#### 服务创建策略
工厂模式确保了服务创建的一致性和可扩展性：

```mermaid
flowchart TD
Request[服务创建请求] --> CheckType{检查服务类型}
CheckType --> |会话服务| CreateConv[创建会话服务实例]
CheckType --> |消息服务| CreateMsg[创建消息服务实例]
CheckType --> |状态机| CreateSM[创建状态机实例]
CheckType --> |WebSocket| CreateWS[创建连接管理器]
CreateConv --> ConfigureConv[配置会话参数]
CreateMsg --> ConfigureMsg[配置消息参数]
CreateSM --> ConfigureSM[配置状态参数]
CreateWS --> ConfigureWS[配置连接参数]
ConfigureConv --> ReturnConv[返回会话服务]
ConfigureMsg --> ReturnMsg[返回消息服务]
ConfigureSM --> ReturnSM[返回状态机]
ConfigureWS --> ReturnWS[返回连接管理器]
```

**图表来源**
- [conversation_service.py:29-51](file://services/gateway/app/services/conversation_service.py#L29-L51)

**章节来源**
- [conversation_service.py:1-179](file://services/gateway/app/services/conversation_service.py#L1-L179)

## 依赖分析

### 组件耦合关系

```mermaid
graph TB
subgraph "核心模块"
StateMachine[state_machine.py]
WSManager[ws_manager.py]
ConversationService[conversation_service.py]
end
subgraph "路由层"
ChatRouter[chat.py]
WSRouter[ws.py]
end
subgraph "工具层"
Deps[deps.py]
JWT[jwt.py]
end
subgraph "数据模型"
ConversationModel[conversation.py]
UserModel[user.py]
end
subgraph "前端"
StudentWS[student-app websocket.ts]
TeacherWS[teacher-app websocket.ts]
UserStore[user.ts]
end
ChatRouter --> StateMachine
ChatRouter --> ConversationService
ChatRouter --> WSManager
WSRouter --> WSManager
StateMachine --> ConversationModel
ConversationService --> ConversationModel
Deps --> JWT
StudentWS --> WSRouter
TeacherWS --> WSRouter
UserStore --> StudentWS
```

**图表来源**
- [chat.py:1-191](file://services/gateway/app/routers/chat.py#L1-L191)
- [ws.py:1-119](file://services/gateway/app/routers/ws.py#L1-L119)
- [state_machine.py:1-96](file://services/gateway/app/services/state_machine.py#L1-L96)

### 依赖循环检测
系统采用了良好的模块化设计，避免了循环依赖：

- **路由层**不直接依赖业务服务，通过依赖注入解耦
- **业务服务**不直接依赖外部 API，通过接口抽象
- **前端**通过标准化的 WebSocket 协议与后端通信

**章节来源**
- [chat.py:1-191](file://services/gateway/app/routers/chat.py#L1-L191)
- [ws.py:1-119](file://services/gateway/app/routers/ws.py#L1-L119)
- [deps.py:1-40](file://services/gateway/app/utils/deps.py#L1-L40)

## 性能考虑

### 状态机优化
- 使用内存中的状态转换表避免重复查询
- 批量操作减少数据库往返次数
- 异步处理提升并发性能

### WebSocket 连接管理
- 连接池复用减少连接建立开销
- 心跳检测及时发现断线
- 断线重连指数退避算法

### 依赖注入优化
- 单例模式减少对象创建成本
- 异步依赖延迟加载
- 连接池管理数据库连接

## 故障排除指南

### 状态机异常处理
当发生非法状态转换时，系统会抛出 `InvalidTransition` 异常：

```mermaid
flowchart TD
Start[状态转换请求] --> Validate[验证转换规则]
Validate --> Valid{转换有效?}
Valid --> |否| ThrowError[抛出 InvalidTransition]
Valid --> |是| UpdateDB[更新数据库状态]
UpdateDB --> InsertSystemMsg[插入系统消息]
InsertSystemMsg --> Broadcast[广播状态变更]
Broadcast --> Commit[提交事务]
Commit --> Success[转换成功]
ThrowError --> LogError[记录错误日志]
LogError --> ReturnError[返回错误]
```

**图表来源**
- [state_machine.py:46-48](file://services/gateway/app/services/state_machine.py#L46-L48)

### WebSocket 连接问题诊断
常见问题及解决方案：

1. **连接失败**: 检查 JWT 令牌有效性
2. **消息丢失**: 验证房间加入状态
3. **断线重连**: 检查指数退避算法
4. **内存泄漏**: 确保连接清理和监听器注销

**章节来源**
- [state_machine.py:8-13](file://services/gateway/app/services/state_machine.py#L8-L13)
- [ws_manager.py:137-149](file://services/gateway/app/services/ws_manager.py#L137-L149)

## 结论

医小管 v2 项目成功应用了多种设计模式来构建一个高性能、可维护的智能校园助手系统。通过状态机模式实现复杂的业务状态管理，通过依赖注入提供灵活的服务解析机制，通过观察者模式实现可靠的实时通信，通过工厂模式确保服务创建的一致性和可扩展性。

这些设计模式的应用不仅提升了系统的架构质量，也为未来的功能扩展和维护提供了坚实的基础。建议在后续开发中继续遵循这些设计原则，确保系统的持续演进和稳定性。