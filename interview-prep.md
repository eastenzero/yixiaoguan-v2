# 医小管 v2 面试亮点文档

> 面向 Java/Python 后端开发实习面试的项目亮点总结

---

## 一、项目一句话介绍

> 医小管 v2 是一个基于 Dify Chatflow 的校园 AI 助手系统，采用 FastAPI 单体 + UniApp 双端架构，支持知识库问答、教师实时介入、意图识别和个性化回答，知识库覆盖 960+ 条校园服务知识。

---

## 二、技术栈亮点

| 层 | 选型 | 面试亮点 |
|---|------|---------|
| 后端 | FastAPI + SQLAlchemy (async) + Alembic | Python 异步框架，类型注解，自动文档生成 |
| AI 引擎 | Dify self-hosted | 自托管 LLM 应用平台，4 分支 Chatflow 架构 |
| 数据库 | PostgreSQL 15+ | 关系型数据库，支持复杂查询和事务 |
| 缓存 | Redis 7+ | 会话管理、缓存加速 |
| 学生端 | UniApp + Vue 3 | 微信小程序 + H5 双编译，跨平台复用 |
| 教师端 | UniApp + Vue 3 + Element Plus | 同上，管理后台 UI 组件库 |
| 向量库 | Dify 内置（通义千问 embedding） | 语义检索，不单独管理向量库 |
| 部署 | Docker Compose | 容器化部署，一键启动 |

---

## 三、核心功能亮点

### 1. AI 多分支对话流（Chatflow）

**技术实现**：
- 使用 Dify 自托管平台构建 4 分支对话流
- 意图分类器（qwen-plus）自动识别用户意图
- 分支路由：闲聊/greeting → 闲聊 LLM、kb_query → 知识检索 → RAG、transfer → 转人工

**面试话术**：
> "我们使用 Dify 构建了 4 分支的 Chatflow 架构。当用户发送消息时，首先通过意图分类器（基于 qwen-plus 模型）识别用户意图，然后路由到对应的处理分支。如果是知识库查询，会经过 RAG 检索后生成回答；如果是转人工，会直接触发工单流程；如果是闲聊，则使用专门的 LLM 进行对话。"

### 2. 知识库管理与 RAG 检索

**技术实现**：
- 960+ 条结构化知识，12 个互斥分类体系
- 使用通义千问 multimodal-embedding-v1 进行向量化
- 语义检索 + qwen3-rerank 重排序，Top K=3
- 支持学院、校区、班级等多维度个性化检索

**面试话术**：
> "知识库包含 960+ 条结构化知识，覆盖教务、后勤、图书馆等 12 个分类。我们使用通义千问的 embedding 模型进行向量化，检索时采用语义检索 + 重排序的两阶段策略。为了提供个性化回答，我们在调用 Dify 时传入用户的学院、校区、班级等上下文信息，让 RAG 优先检索与该学生相关的内容。"

### 3. 实时通信与状态机

**技术实现**：
- WebSocket/Centrifugo 实现实时消息推送
- 5 种会话状态：AI 服务、待教师接入、教师服务、已解决、已关闭
- 教师接入后停止调用 Dify，学生消息改为实时推送给教师端
- 状态变化实时同步到学生端和教师端

**面试话术**：
> "我们设计了一个完整的会话状态机，支持 AI 自动服务和人工服务的无缝切换。当 AI 无法解决问题时，学生可以请求转人工，系统会创建工单并通知教师。教师接入后，会话状态从'AI 服务'变为'教师服务'，此时学生发送的消息会通过 WebSocket 实时推送给教师，而不是继续调用 AI。这种设计保证了人机协作的流畅性。"

### 4. 教师实时介入

**技术实现**：
- 教师可以随时"闯入"正在进行的 AI 对话
- 对话气泡明确标识消息来源（AI 还是真实教师）
- 教师插入后，AI 暂停响应，学生消息直接推送给教师
- 教师退出后，可选择恢复 AI 服务或关闭会话

**面试话术**：
> "我们实现了教师实时介入功能。教师可以在任何时候插入学生与 AI 的对话，此时对话界面会明确标识消息来源。教师介入后，AI 会暂停响应，学生的新消息会通过 WebSocket 实时推送给教师端。这种设计让教师能够在关键时刻提供精准指导，同时保持了对话的连续性。"

### 5. 意图识别与快捷入口

**技术实现**：
- AI 自动识别用户意图（查询、办事、转人工等）
- 根据意图类型推荐相关快捷入口和链接
- 支持图文教程卡片渲染（如电费缴纳、校园卡充值等）

**面试话术**：
> "系统不仅能回答用户问题，还能识别用户意图并推荐相关服务入口。例如当学生问'怎么交电费'时，AI 会识别出这是一个办事意图，除了提供文字说明外，还会在前端渲染图文教程卡片，展示具体的操作步骤截图。这种设计将问答与服务入口打通，提升了用户体验。"

### 6. 数据分析与知识库优化

**技术实现**：
- 记录每次对话的 RAG 检索分数和命中情况
- 统计高频无答案问题，发现知识库盲区
- 支持教师草稿、管理员审核、发布到 Dify Dataset 的知识维护流程

**面试话术**：
> "我们建立了数据分析机制来持续优化知识库。系统会记录每次对话的 RAG 检索结果，包括匹配分数和命中文档。通过分析高频无答案问题，我们可以发现知识库的盲区，有针对性地补充内容。同时，教师可以直接在系统中创建知识草稿，经过管理员审核后发布到 Dify Dataset，形成知识库的持续迭代闭环。"

---

## 四、技术深度亮点

### 1. 异步架构设计

**面试话术**：
> "FastAPI 基于 asyncio 实现异步处理，我们使用 SQLAlchemy 的异步模式（async session）来操作数据库，避免了同步阻塞。在处理 Dify 的 SSE 流式响应时，我们使用异步生成器逐块读取并转发给前端，保证了长连接场景下的性能。"

### 2. 数据库设计与迁移

**面试话术**：
> "我们使用 SQLAlchemy 定义数据模型，通过 Alembic 管理数据库迁移脚本。核心表包括用户表、会话表、消息表、知识条目表、公告表、反馈表等。在设计时考虑了索引优化，比如会话表按用户 ID 和更新时间建立联合索引，保证查询效率。"

### 3. 容器化部署

**面试话术**：
> "项目使用 Docker Compose 进行容器化部署，将 FastAPI Gateway、Dify、PostgreSQL、Redis 等服务编排在一起。通过环境变量和配置文件管理不同环境的配置，实现了开发、测试、生产环境的一致性。"

### 4. 跨平台前端开发

**面试话术**：
> "学生端和教师端都使用 UniApp + Vue 3 开发，可以同时编译为微信小程序和 H5。我们使用条件编译处理平台差异，比如在微信小程序中使用 wx.request，在 H5 中使用 axios。这种方案大大降低了多端开发的成本。"

---

## 五、高频面试问答

### Q1：为什么选择 FastAPI 而不是 Spring Boot？

**推荐回答**：
> "选择 FastAPI 主要基于三个考虑：第一，FastAPI 原生支持异步，处理 SSE 流式响应和 WebSocket 更自然；第二，FastAPI 的类型注解和自动文档生成能力，让 API 开发和调试更高效；第三，项目需要深度集成 Dify 的 Python SDK，使用 Python 技术栈可以避免跨语言调用的复杂性。当然，Spring Boot 在企业级应用中更成熟，我们也在项目中使用了 Spring Boot 构建教师端的管理后台。"

### Q2：Dify 和直接调用 OpenAI API 有什么区别？

**推荐回答**：
> "Dify 是一个 LLM 应用开发平台，它提供了可视化的 Chatflow 编排、知识库管理、RAG 检索、对话历史管理等能力。直接调用 OpenAI API 只是模型调用层，我们需要自己实现意图识别、知识检索、对话管理等逻辑。使用 Dify 可以快速构建复杂的对话流，同时支持自托管，保证了数据安全。"

### Q3：如何保证 AI 回答的准确性？

**推荐回答**：
> "我们通过三个机制保证准确性：第一，使用 RAG 检索知识库，让 AI 基于事实回答，减少幻觉；第二，设置检索分数阈值，当匹配度低于阈值时，系统会提示用户问题超出范围并建议转人工；第三，建立知识库迭代机制，通过分析高频无答案问题，持续补充和优化知识库内容。"

### Q4：WebSocket 在项目中的作用是什么？

**推荐回答**：
> "WebSocket 主要用于三个场景：第一，教师介入时，学生消息需要实时推送给教师端；第二，会话状态变化（如教师接单、会话关闭）需要实时同步到双方；第三，AI 流式回答需要实时推送给前端。我们使用 Centrifugo 作为 WebSocket 服务端，它支持集群部署和消息持久化，保证了消息的可靠传递。"

### Q5：如何处理多租户（不同学院、校区）的数据隔离？

**推荐回答**：
> "我们在用户表中存储了学院、校区、班级等属性信息。在调用 Dify 时，将这些信息作为 inputs 参数传入，Dify 的 prompt 中会引导 AI 优先使用与该学生相关的信息回答。同时，在知识库建设时，我们为每个知识点标注了适用的学院和校区，RAG 检索时会优先匹配相关标签的内容。"

### Q6：项目中遇到的最大挑战是什么？

**推荐回答**：
> "最大的挑战是实现 AI 服务和人工服务的无缝切换。当 AI 无法解决问题时，学生可以请求转人工，此时需要：1）创建工单并通知教师；2）停止调用 AI，将学生消息实时推送给教师；3）教师可以随时介入或退出；4）会话状态变化需要实时同步到双方。我们通过设计状态机和 WebSocket 通信机制，实现了这个复杂的交互流程。"

### Q7：如何保证系统的可扩展性？

**推荐回答**：
> "我们在架构设计时考虑了可扩展性：第一，Gateway 采用模块化设计，14 个路由模块各自独立，新增功能只需添加新模块；第二，AI 引擎使用 Dify，修改对话流只需在 Dify 控制台操作，无需改代码；第三，知识库通过 API 管理，支持批量导入和增量更新；第四，前端使用组件化开发，新页面可以复用现有组件。"

### Q8：如果让你重新设计，会做哪些改进？

**推荐回答**：
> "我会做三个改进：第一，引入消息队列（如 Redis Stream 或 Kafka），将耗时的操作（如知识库更新、数据分析）异步化，提高系统响应速度；第二，实现更细粒度的权限控制，区分不同角色（学生、教师、管理员）的访问权限；第三，增加监控和告警机制，实时监控系统状态和 AI 回答质量。"

---

## 六、项目数据亮点（可用于简历量化）

| 指标 | 数值 | 说明 |
|------|------|------|
| API 路由模块 | 14 个 | 认证、会话、消息、公告、知识库、后台管理、数据统计等 |
| 知识库条目 | 960+ 条 | 覆盖 12 个分类体系 |
| 会话状态 | 5 种 | AI 服务、待教师接入、教师服务、已解决、已关闭 |
| Chatflow 分支 | 4 个 | 闲聊、知识查询、转人工、兜底处理 |
| 前端应用 | 2 个 | 学生端 + 教师端，支持微信小程序和 H5 |
| 测试模块 | 12 个 | pytest 覆盖核心业务分支 |

---

## 七、简历优化建议

### 当前简历描述（可优化）

> 参与 FastAPI Gateway 开发，完成认证、会话、消息、公告、知识库、后台管理、数据统计等 14 个路由模块的接口组织和联调。

### 优化后描述（突出亮点）

> **主导** FastAPI Gateway 开发，设计并实现 14 个路由模块，涵盖认证、会话管理、实时消息、公告通知、知识库维护、后台管理和数据分析；**封装** Dify Chatflow 流式接口，实现 4 分支意图识别与 RAG 检索，知识库覆盖 960+ 条校园服务知识；**设计** 5 状态机会话模型，支持 AI 自动服务与教师实时介入的无缝切换；**使用** WebSocket/Centrifugo 实现学生端与教师端的实时消息同步。

### 关键优化点

1. **动词强化**：将"参与"改为主导、设计、实现、封装等更有力度的动词
2. **数据量化**：突出 14 个模块、960+ 条知识、5 种状态等具体数据
3. **技术深度**：提到 Dify Chatflow、RAG、状态机、WebSocket 等技术细节
4. **业务价值**：强调 AI 与人工协作、实时通信、知识库迭代等业务价值

---

## 八、面试准备清单

### 技术知识点

- [ ] FastAPI 异步编程和依赖注入
- [ ] SQLAlchemy ORM 和数据库迁移
- [ ] WebSocket 原理和 Centrifugo 架构
- [ ] RAG 检索原理和向量数据库
- [ ] SSE 流式响应实现
- [ ] Docker Compose 容器化部署
- [ ] UniApp 跨平台开发

### 项目细节

- [ ] 会话状态机的设计和实现
- [ ] Dify Chatflow 的配置和调用方式
- [ ] 知识库的结构和分类体系
- [ ] 教师介入的交互流程
- [ ] 数据分析和知识库优化机制

### 面试技巧

- [ ] 使用 STAR 法则描述项目经历
- [ ] 准备 2-3 个技术难点的解决方案
- [ ] 量化项目成果（如 14 个模块、960+ 条知识）
- [ ] 准备项目改进方向的回答

---

*创建日期：2026-06-02*
*基于医小管 v2 项目代码和文档整理*
